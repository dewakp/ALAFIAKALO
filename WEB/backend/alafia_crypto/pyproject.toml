[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "alafia_crypto"
version = "0.1.0"
description = "High-performance Rust cryptographic services for ALAFIA"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
