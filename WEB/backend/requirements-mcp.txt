# Dependencies for the standalone ALAFIA Nutrition MCP server
# (app/services/mcp_nutrition_server.py).
#
# Kept separate from requirements.txt on purpose: fastmcp pulls a newer Starlette
# than FastAPI 0.115.6 allows, so the MCP server runs in its own image/process
# (see WEB/backend/Dockerfile.mcp + the `mcp` service in WEB/docker-compose.yml)
# rather than being mounted on the FastAPI app. The MCP import chain
# (config → database → med_nutrient_service → models) is FastAPI-free, so this
# minimal set is sufficient.

fastmcp>=2.0,<3
httpx>=0.27
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2
pydantic-settings>=2
python-dotenv>=1
