# FastAPI & Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.10

# Auth — PyJWT (maintained) + hybrid PQC (dilithium-py); Argon2id passwords (passlib[argon2])
PyJWT[crypto]==2.10.1
dilithium-py==1.4.0
passlib[bcrypt,argon2]==1.7.4
bcrypt==4.2.1

# Validation & Settings
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# CORS & HTTP
httpx==0.28.1

# Rate Limiting
slowapi==0.1.9

# AI / LLM
openai==1.58.1

# Firebase
firebase-admin==6.8.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0

# PDF generation
reportlab==4.2.5

# Utilities
python-dateutil==2.9.0

# Blockchain / On-chain anchoring
web3>=6.19.0

# Task scheduling
apscheduler>=3.11.0

# Error tracking
sentry-sdk[fastapi]==2.25.1

# Object storage (S3-compatible)
boto3==1.38.12

# Redis (WebSocket pub/sub)
redis[hiredis]==5.2.1

# MCP (Model Context Protocol) — nutrition server + in-process tool calls
# fastmcp>=2.0.0 conflicts with fastapi 0.115.6 (starlette version clash), so it is
# NOT installed here. The nutrition tools remain callable in-process without it, and
# the MCP protocol endpoint runs in its own container (requirements-mcp.txt +
# Dockerfile.mcp + the `mcp` service in WEB/docker-compose.yml). Install fastmcp into
# this image only after bumping FastAPI to a Starlette-compatible version.
